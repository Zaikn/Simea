<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.simea.simea">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DictionnaryActivity"
            android:label="@string/title_activity_dictionnary"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SignatureActivity"
            android:label="@string/title_activity_signature"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MissionDetails"
            android:label="@string/title_activity_mission_details"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MissionActivity"
            android:label="@string/title_activity_mission"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>